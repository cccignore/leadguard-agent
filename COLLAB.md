# 单人开发与 AI 协作记录

## 参赛者

- 姓名：陈一诺
- 联系方式：1063919295@qq.com（GitHub: cccignore）
- 代表提交：仓库 `main` 分支全部 commit（最终 commit 见提交历史顶端）
- 人工投入：主要在需求确认、方向决策、逐轮验收与答辩准备，未单独精确计时；AI 辅助开发全程记录于本文件

**关于组队要求的说明**：题目要求两人一组；本次实际为单人作答，如实披露，不虚构第二名成员或协作提交历史。是否接受单人形式以题方答复为准；若已另行沟通获得许可，将在答辩时说明。

## 实际开发范围

- 后端：领域状态机、SQLite 事务、统一发送边界、Provider 适配器与 API。
- 前端：本地多客户对话控制台、服务端审计轨迹和隔离攻击演示。
- 验证：并发/幂等/安全测试、真实 LLM acceptance、浏览器 QA 与模型评测。
- 文档：一页 README、约束边界、攻击结果、live 报告和模型评测报告。

## 关键技术决策

- 直接调用受限 LLM Provider，而不采用通用 Agent 框架：模型无工具权限，服务端完整控制动作边界。
- 以 SQLite 写事务实现状态复检和任意 60 秒滑动窗口，符合本地、小吞吐 demo 规模。
- 分离“模型建议”和“实际动作”；连续异常、静默、限流和防泄漏均能覆盖模型输出。
- 操作员控制面（恢复/重置/诊断）与客户消息面分离：`OPERATOR_TOKEN` 环境变量启用令牌鉴权，enforced 模式下客户面只返回公开结果。
- 选择 `gpt-5.6-luna` 作为默认模型：同一 smoke 中交互延迟显著低于 Terra，安全边界保持不变。

## AI 工具使用

- 用途：需求拆解、实现草案、独立 code review、测试设计、真实评测与文档协助（Codex 与 Claude 分轮次参与）。
- 人工必须完成：理解每个硬约束、复跑测试、审阅真实输出、演练答辩并对最终提交负责。
- 已修正的 AI/实现问题：并发消息顺序、follow-up 标记丢失、UI 恢复后状态投影、攻击场景串状态、Provider 墙钟超时、健康状态误报等。

## 外部审查后的修正轮（提交前）

一次独立的外部 AI 代码审查指出三项问题，均已修复并补测试：

1. **“人工重新激活”缺少可信身份边界（P1）**：`/reactivate`、`/api/demo/reset`、会话列表/详情原先无鉴权，匿名调用可在转人工后恢复自动执行。修复：新增 `OPERATOR_TOKEN` 令牌鉴权（常数时间比对），enforced 模式下上述接口匿名一律 401，客户消息接口只返回 `{request_id, outcome, reply}` 公开视图，诊断数据（意图、覆写、guard trace）仅对持令牌调用可见；未配置令牌时按本地演示 open 模式运行并在 `/api/health` 标注。UI 收到 401 会提示输入令牌。
2. **中断请求永久占用幂等键（P2）**：崩溃发生在“入站已写、结果未写”之间时，相同 `request_id` 重试会永远 409。修复：同内容重放被接受并继续处理至完成；`request_id` 换内容复用才拒绝（409），已完成 turn 的重放同样校验内容一致。
3. **Gemini 适配器缺少墙钟超时（P2）**：为每次请求加 `asyncio.timeout(MODEL_TIMEOUT_SECONDS)`，挂起的 Provider 不再能长期占用会话锁。

修正轮由 Claude 辅助完成（约 1 小时）；`uv run pytest`（61 通过 / 2 live 跳过）、`ruff check`、`mypy src/leadguard` 全部通过。

## 已知边界

- 当前按单 ASGI 进程运行；多 worker 需要数据库序号与每客户队列。
- 控制面为单令牌方案；生产部署应升级为账号体系 + RBAC + 审计。
- 自然语言防泄漏无法形式化保证 100%，已采用数据最小化、确定性检查、语义 reviewer 和失败关闭。

## 人工确认边界（如实声明）

以上验证由 AI 执行、结果如实记录；提交者应在答辩前亲自跑通 README 全部命令、读完 `service.py` / `storage.py` / `domain.py` 与四条硬约束的对应测试，并能独立解释 `docs/CONSTRAINTS.md` 中每条防线的位置与局限。本文件不代替该项工作，也不虚构其已完成。
